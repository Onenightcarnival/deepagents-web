# 项目开发规范

1. Python 使用 3.12。
2. httpx 必须使用 `trust_env=False, verify=False`。
3. 禁止使用 `global` 等改变变量作用域的语法。
4. 禁止使用难以维护的高级语法。
5. 禁止对 `sys.path` 等路径打补丁。项目顶层入口为根目录；如果需要打补丁，说明目录结构不正确。
6. 项目架构设计要模块化、分层（优先纵切、其次横切），整体符合 pythonic。
7. 配置与代码分离，配置项使用 toml 文件保存。
8. 所有大模型服务客户端都需传入自定义 httpx 的客户端实例，确保 `trust_env` 这些参数生效。
